import type { Weapon, WeaponBase, WeaponStage } from './builderWeapon';

export default class BuildManager {

  stage1?: WeaponBase;
  stage2?: WeaponStage;
  stage3?: WeaponStage;
  stage4?: WeaponStage;
  stage5?: WeaponStage;
  stage6?: WeaponStage;
  stage7?: WeaponStage;
  stage8?: Weapon;

  /**
   * Standard constructor.
   */
  constructor() {}
}